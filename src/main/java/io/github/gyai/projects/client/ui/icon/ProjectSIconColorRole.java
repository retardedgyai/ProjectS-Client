package io.github.gyai.projects.client.ui.icon;

/** Semantic color requested by an icon definition or its call site. */
public enum ProjectSIconColorRole {
    DEFAULT,
    PRIMARY,
    SECONDARY,
    MUTED,
    DISABLED,
    SUCCESS,
    WARNING,
    DANGER,
    INFO
}
