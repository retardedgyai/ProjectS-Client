package io.github.gyai.projects.client.ui.icon;

/** Interaction state used when resolving a theme tint. */
public enum ProjectSIconState {
    NORMAL,
    HOVERED,
    SELECTED,
    FOCUSED,
    DISABLED
}
