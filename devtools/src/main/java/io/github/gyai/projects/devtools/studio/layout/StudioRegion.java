package io.github.gyai.projects.devtools.studio.layout;

/** Stable region identifiers shared by the ProjectS Studio shell composition. */
public enum StudioRegion {
    TOP_BAR,
    TOOL_PALETTE,
    VIEWPORT,
    INSPECTOR,
    TIMELINE,
    ASSET_BROWSER
}
