package io.github.gyai.projects.devtools.studio.timeline;

/** Commands exposed by the Studio playback transport. */
public enum StudioPlaybackAction {
    RESTART,
    PLAY_PAUSE,
    STOP,
    LOOP,
    SPEED
}
