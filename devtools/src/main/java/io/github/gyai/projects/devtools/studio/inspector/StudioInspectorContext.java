package io.github.gyai.projects.devtools.studio.inspector;

/** Contexts rendered by the Stage 3 presentation-only inspector shell. */
public enum StudioInspectorContext {
    NO_SELECTION,
    PRIMITIVE,
    EMISSION,
    HANDLE
}
