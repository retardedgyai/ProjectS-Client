package io.github.gyai.projects.devtools.studio;

/** Studio overlays whose open transition must cancel viewport ownership. */
public enum StudioOverlayKind {
    THEME_POPUP,
    ASSET_BROWSER,
    INSPECTOR_DRAWER
}
