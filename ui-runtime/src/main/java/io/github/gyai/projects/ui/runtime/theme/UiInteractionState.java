package io.github.gyai.projects.ui.runtime.theme;

/** Shared component state vocabulary used when resolving stateful theme tokens. */
public enum UiInteractionState {
    NORMAL,
    HOVER,
    PRESSED,
    FOCUSED,
    SELECTED,
    DISABLED
}
